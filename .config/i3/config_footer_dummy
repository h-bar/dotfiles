# vim: filetype=i3config

# This will be added to begiding of your config file
# Copy this file as `config_footer` and add your modifications to it
# Also you can put your overrides in it.

## Footer

# Extra Startup
exec --no-startup-id bash ~/.config/i3/lock.sh &
exec --no-startup-id /usr/lib/kdeconnectd
exec --no-startup-id indicator-kdeconnect
exec blueman-applet

# Dropdown
exec $term -name dropdown
for_window [instance="dropdown"] floating enable
for_window [instance="dropdown"] move position 0px 20px
for_window [instance="dropdown"] resize set 1366px 400px
for_window [instance="dropdown"] move scratchpad
bindsym Ctrl+space [instance="dropdown"] scratchpad show

# Modification per WS
workspace $ws2 gaps inner 0
workspace $ws4 gaps inner 0
